# Production env vars are set in the Vercel dashboard (Project → Settings →
# Environment Variables), NOT in this file. The frontend uses same-origin /api
# routes, so NEXT_PUBLIC_API_URL is not required.

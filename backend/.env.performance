# MongoDB Performance Optimization
# Add this to your MongoDB configuration file (mongod.conf) or connection string

# For MongoDB Atlas users:
# 1. Ensure you're using the nearest region
# 2. Use connection pooling
# 3. Enable read preference for secondary reads

# For local MongoDB, add this configuration:

# Connection String Optimization
MONGODB_URI=mongodb://localhost:27017/notes-ai?maxPoolSize=50&w=majority&retryWrites=true

# Or if using MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/notes-ai?retryWrites=true&w=majority&maxPoolSize=50&minPoolSize=5

# Environment Variables for Performance
MONGODB_MAX_POOL_SIZE=50
MONGODB_MIN_POOL_SIZE=5
MONGODB_CONNECT_TIMEOUT=10000
MONGODB_SOCKET_TIMEOUT=30000
MONGODB_SERVER_SELECTION_TIMEOUT=10000
MONGODB_HEARTBEAT_FREQUENCY=10000

# Enable MongoDB logging (for debugging slow queries)
MONGODB_LOG_LEVEL=warn
